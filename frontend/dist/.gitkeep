# This placeholder keeps frontend/dist/ present so `//go:embed all:frontend/dist`
# in main.go compiles in CI/test jobs that run `go vet`/`go test` without first
# building the frontend. Real build artifacts here are gitignored; `wails build`
# repopulates this directory before embedding for actual app builds.
